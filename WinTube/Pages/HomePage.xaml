<Page
    x:Class="WinTube.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinTube.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:Mail.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <SymbolIcon VerticalAlignment="Center" Symbol="Home"/>
            <TextBlock Margin="10, 0, 0, 0" Style="{StaticResource HeaderTextBlockStyle}" Text="Home"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Style="{StaticResource HeaderTextBlockStyle}" Text="WinTube" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBlock Grid.Row="2" Margin="0, 10, 0, 0" Style="{StaticResource CaptionTextBlockStyle}" Text="v 1.0" HorizontalAlignment="Right"/>
    </Grid>
</Page>