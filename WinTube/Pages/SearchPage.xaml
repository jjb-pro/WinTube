<Page
    x:Class="WinTube.Pages.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinTube.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:observableModel="using:WinTube.Model.Observable"
    xmlns:viewmodels="using:WinTube.ViewModels"
    xmlns:converters="using:WinTube.Converters"
    d:DataContext="{d:DesignInstance Type=viewmodels:SearchViewModel}"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <AutoSuggestBox VerticalAlignment="Center" QueryIcon="Find" Text="{x:Bind ViewModel.SearchText, Mode=TwoWay}" QuerySubmitted="{x:Bind ViewModel.OnQuerySubmitted}"/>
            <Button Grid.Column="1" Margin="5, 0, 0, 0" VerticalAlignment="Center" Background="Transparent">
                <SymbolIcon Symbol="Filter"/>
            </Button>
        </Grid>
        <ProgressBar Grid.Row="1" Margin="0, 10, 0, 0" IsIndeterminate="True" Visibility="{x:Bind ViewModel.IsProgressBarVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <StackPanel Visibility="{x:Bind ViewModel.IsOffline, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" Grid.Row="2" Margin="0, 10, 0, 0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <FontIcon Glyph="&#xF384;" FontSize="32"/>
            <TextBlock Margin="0, 10, 0, 0" Style="{StaticResource TitleTextBlockStyle}" Text="It looks like you're currently offline."/>
        </StackPanel>

        <ListBox Visibility="{x:Bind ViewModel.IsOffline, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=true}" Grid.Row="2" Margin="0, 10, 0, 0" ItemsSource="{x:Bind ViewModel.SearchResults, Mode=OneWay}" Background="Transparent" SelectionChanged="ListBox_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate x:DataType="observableModel:ObservableVideoSearchResult">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>

                        <Grid>
                            <Image Source="{x:Bind Thumbnail, Mode=OneWay}" Stretch="Fill"/>

                            <Border Background="Black" HorizontalAlignment="Right" VerticalAlignment="Bottom" Padding="5" Margin="5" CornerRadius="5">
                                <TextBlock Text="{x:Bind Duration}" Foreground="White" FontSize="12" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>

                        <Grid Grid.Row="1" Margin="0, 10, 0, 0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            
                            <Grid Grid.RowSpan="1" Width="32" Height="32" VerticalAlignment="Center">
                                <Ellipse Fill="Gray" StrokeThickness="1"/>
                                <SymbolIcon HorizontalAlignment="Center" VerticalAlignment="Center" Symbol="Contact" Foreground="White"/>
                            </Grid>

                            <Grid Grid.Column="1" Margin="10, 0, 0, 0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock FontWeight="Bold" TextTrimming="WordEllipsis" Text="{x:Bind Title}"/>
                                <TextBlock Grid.Row="1" Margin="0, 2, 0, 0" Style="{StaticResource CaptionTextBlockStyle}" Text="{x:Bind ChannelTitle}"/>
                            </Grid>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>