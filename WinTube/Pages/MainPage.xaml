<Page
    x:Class="WinTube.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:WinTube.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:WinTube.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{StaticResource AppBackgroundBrush}"
    Loaded="OnLoaded"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid Padding="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <Image
                    Width="32"
                    VerticalAlignment="Center"
                    Source="/Assets/Template/WinTube_Icon.png" />
                <TextBlock
                    Margin="8,0,0,3"
                    VerticalAlignment="Center"
                    Style="{StaticResource TitleTextBlockStyle}"
                    Text="WinTube" />
            </StackPanel>

            <StackPanel
                Grid.Column="2"
                Orientation="Horizontal"
                Spacing="10">
                <Button
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Background="Transparent"
                    BorderThickness="0">
                    <controls:FontIcon Glyph="&#xE721;" />
                </Button>
                <Button
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Background="Transparent"
                    BorderThickness="0">
                    <controls:FontIcon Glyph="&#xEA8F;" />
                </Button>
                <controls:ChannelPicture
                    Padding="20,10,10,10"
                    ImageSource="/Assets/Template/temp-channel.png"
                    Size="40" />
            </StackPanel>


            <!--<AutoSuggestBox
                VerticalAlignment="Center"
                QueryIcon="Find"
                QuerySubmitted="{x:Bind ViewModel.OnQuerySubmitted}"
                Text="{x:Bind ViewModel.SearchText, Mode=TwoWay}" />
            <Button
                Grid.Column="1"
                Margin="5,0,0,0"
                VerticalAlignment="Center"
                IsEnabled="False">
                <SymbolIcon Symbol="Setting" />
            </Button>-->
        </Grid>

        <Frame x:Name="contentFrame" Grid.Row="1" />
    </Grid>
</Page>