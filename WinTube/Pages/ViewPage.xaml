<Page
    x:Class="WinTube.Pages.ViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:WinTube.Controls"
    xmlns:converters="using:WinTube.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:WinTube.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:WinTube.ViewModels"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">
    <!--<Page.Resources>
        <converters:PlayBoolToIconConverter x:Key="PlayBoolToIconConverter"/>
    </Page.Resources>-->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <controls:FontIcon Glyph="&#xF384;" />

        <controls:SynchronizedMediaControl x:Name="mediaControl" />

        <!--<MediaPlayerElement
            x:Name="audioPlayerElement"
            AreTransportControlsEnabled="True"
            Source="{x:Bind ViewModel.VideoMediaSource, Mode=OneWay}" />
        <MediaPlayerElement
            x:Name="mediaControl"
            AreTransportControlsEnabled="True"
            Source="https://dash.akamaized.net/akamai/bbb_30fps/bbb_30fps.mpd" />-->

        <!--<MediaPlayerElement x:Name="audioPlayerElement" Source="{x:Bind ViewModel.VideoMediaSource, Mode=OneWay}" AreTransportControlsEnabled="True">
            <MediaPlayerElement.TransportControls>
                <controls:CustomMediaTransportControls x:Name="customMediaTransportControls" PlayClicked="customMediaTransportControls_PlayClicked" ProgressSliderValueChanged="Slider_ValueChanged">

                </controls:CustomMediaTransportControls>
            </MediaPlayerElement.TransportControls>
        </MediaPlayerElement>
        <MediaPlayerElement Grid.Row="1" x:Name="videoPlayerElement" Source="{x:Bind ViewModel.AudioMediaSource, Mode=OneWay}"/>-->
        <!--
        <Grid
            Grid.Row="2"
            Margin="0,10,0,0"
            Padding="10"
            BorderBrush="{ThemeResource SystemAccentColor}"
            BorderThickness="1"
            CornerRadius="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <SymbolIcon VerticalAlignment="Center" Symbol="Cancel" />
            <TextBlock
                Grid.Column="1"
                Margin="10,0,0,0"
                VerticalAlignment="Center"
                Text="An error has occured while loading the media source. Try selecting a different one."
                TextWrapping="WrapWholeWords" />
        </Grid>-->

        <TextBlock
            Grid.Row="3"
            Margin="0,10,0,0"
            Style="{StaticResource TitleTextBlockStyle}"
            Text="{x:Bind ViewModel.Title, Mode=OneWay}" />
    </Grid>
</Page>